{% extends 'base.html.twig' %}

{% block title %}Pyam - {{ parent() }}{% endblock %}

{% block body %}
    <div class="container my-5">
        <div class="row">
            <div class="col-md-4">
                {{ form_start(form) }}


                <div class="input-group mb-3">
                    {{ form_widget(form.date) }}
                    <button type="submit" class="btn btn-primary">
                        Go
                    </button>
                </div>


                {{ form_end(form) }}
            </div>
        </div>
{#        <ul class="list-group">#}
{#            {% for datum in data %}#}
{#                <li class="list-group-item">De {{ datum.from|date('H:i:s') }} à {{ datum.to|date('H:i:s') }}</li>#}
{#            {% endfor %}#}
{#        </ul>#}
        <table class="table table-bordered table-striped">
            <thead>
            <tr>
                <th>De</th>
                <th>à</th>
            </tr>
            </thead>
       <tbody>
       {% for datum in data %}
           <tr>
               <td>{{ datum.from|date('H:i:s') }}</td>
               <td>{{ datum.to|date('H:i:s') }}</td>
           </tr>
       {% endfor %}
       </tbody>
        </table>
    </div>
{% endblock %}
